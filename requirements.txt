# OCR相关
paddlepaddle==3.0.0
paddleocr>=3.0.0
pymupdf>=1.25.2


# 图像处理
numpy>=1.24.3,<2.0.0
img2table>=1.3.0

# 向量和检索
lancedb==0.16.0
sentence-transformers==5.5.1
transformers>=4.41.0,<5.0.0     # 调整下限
torch>=2.6.0                    #  transformers 安全策略要求 ≥2.6，BGE-M3 使用 safetensors
torchvision>=0.21.0             #  与 torch 2.6.x 配套
torchaudio>=2.6.0               #  与 torch 2.6.x 配套
huggingface-hub>=0.19.0

# 表格识别 (PP-StructureV3) 新增依赖
paddlex[ocr]>=3.0.0

# 工具
pandas>=2.2.0
pydantic>=2.5.0
tqdm>=4.66.1
beautifulsoup4>=4.12.0  # 解析表格 HTML

# LLM
# ── 关于 openai 包 ──
# 本项目仅使用 DeepSeek API。但因 DeepSeek 与 OpenAI 协议完全兼容，
# 业界标准做法是使用更成熟的 `openai` Python 包作为 HTTP 客户端，
# 通过 base_url 指向 DeepSeek 服务器。请求不会经过 OpenAI。
openai>=1.6.1

# 前端
streamlit>=1.35.0